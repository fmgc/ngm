<?xml version = "1.0" encoding = "ISO-8859-1" ?>

<!--

This file was generated by Jason 1.3.9-alpha
http://jason.sf.net

Julho 19, 2013 - 11:04:55

And later updated by fc@di.uevora.pt

Wed Jan  8 14:45:07 WET 2014

and 

Sáb 28 Fev 2015 12:29:55 UTC

-->

<project name   = "noisy_gold_miners"
    basedir = "."
    default = "run">

    <property name = "mas2j.project.file" value = "noisy_gold_miners.mas2j"/>
    <property name = "debug" value = ""/>
    <property name = "build.dir" value = "${basedir}/bin" />
    <property name = "java.source.dir" value = "${basedir}/src/java" />

    <property name = "tool.base.dir" value = "${basedir}/lib"/>
    <property name = "jasonJar" value = "${tool.base.dir}/jason.jar"/>

    <!-- property name = "runner" value = "jason.infra.centralised.RunCentralisedMAS"/ -->
    <property name = "runner" value = "infra.NoisyGoldMinersRunner"/>

    <path id = "project.classpath">
        <pathelement location = "${basedir}"/>
        <pathelement location = "${build.dir}"/>
        <pathelement location = "${jasonJar}"/>
        <fileset dir = "${basedir}/lib" >
            <include name = "*.jar" />
        </fileset>

    </path>

    <!-- tasks the user can override in his/her c-build.xml script -->
    <target name = "user-init">
    </target>
    <target name = "user-end">
    </target>

    <target name = "init">
        <mkdir dir = "${build.dir}" />
        <antcall target = "user-init" />
    </target>

    <target name = "compile" depends = "init">
        <javac srcdir = "${java.source.dir}" destdir = "${build.dir}" debug = "true" optimize = "true" includeantruntime = "false" >
            <classpath refid = "project.classpath"/>
        </javac>
    </target>

    <target name = "jar" depends = "compile">
        <delete file = "${ant.project.name}.jar" />
        <copy file = "${jasonJar}" tofile = "${ant.project.name}.jar" />
        <copy file = "${mas2j.project.file}" tofile = "default.mas2j" />
        <jar update = "yes" jarfile = "${ant.project.name}.jar" >
            <fileset dir = "${basedir}">
                <include name = "**/*.asl" />
                <include name = "**/*.mas2j" />
            </fileset>
            <fileset dir = "${build.dir}">
                <include name = "**/*.class" />
            </fileset>
            <manifest>
                <attribute name = "Main-Class" value = "${runner}"/>
            </manifest>
        </jar>
        <delete file = "default.mas2j" />
    </target>

    <target name = "run" depends = "compile" >
        <echo message = "Running project ${ant.project.name}" />
        <java classname = "${runner}"
            failonerror = "true" fork = "yes" dir = "${basedir}" >
            <classpath refid = "project.classpath"/>
            <arg line = "${mas2j.project.file} ${debug} "/>
            <!-- jvmarg line = "-Xmx500M -Xss8M"/ -->    
        </java>
        <antcall target = "user-end" />
    </target>

    <target name = "clean" >
        <delete failonerror = "no" includeEmptyDirs = "true" verbose = "true">
            <fileset dir = "${basedir}" includes = "**/*.class"/>
        </delete>
    </target>

    <target name = "test" depends = "compile" >
        <echo message = "Testing project ${ant.project.name}" />
        <java classname = "test.BNCalcTest"
            failonerror = "true" fork = "yes" dir = "${basedir}" >
            <classpath refid = "project.classpath"/>
            <arg line = "${mas2j.project.file} ${debug} "/>
            <!-- jvmarg line = "-Xmx500M -Xss8M"/ -->    
        </java>
        <antcall target = "user-end" />
    </target>

</project>
